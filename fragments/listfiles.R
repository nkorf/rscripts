# Code to list files in a directory 

#File directory location can be specified as a separate variable

file_list <- list.files("~/Dropbox/")

# Usefull info the ~ (pronounced as tidle) specificies the home 
# path in UNIX like systems 

# For Windows Systems the C:\Users\username is the equivalent directory

file_list 
